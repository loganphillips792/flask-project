POSTHOG_PROJECT_TOKEN=<SETINGS/PROJECT_DETAILS>
POSTHOG_HOST=https://us.i.posthog.com
# Flask secret key — set a real value in production; a dev-only fallback is used if unset.
SECRET_KEY=<GENERATE_WITH: python -c "import secrets; print(secrets.token_hex(32))">


